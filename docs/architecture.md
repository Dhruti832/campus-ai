# Architecture

**Status: placeholder — filled in as Phase 1 modules land.**

This document will hold the request-flow diagram (crawler → chunker → embeddings → pgvector retrieval → LLM provider → FastAPI → Next.js UI) and the per-module responsibility breakdown, in the same spirit as the original project's `docs/DesignPrinciples.md`. See [`docs/adr/`](adr/) for the individual technology decisions and their rationale in the meantime.
